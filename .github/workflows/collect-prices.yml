name: Collect Fuel Prices

on:
  schedule:
    - cron: '0 */8 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger price collection
        run: curl -f https://precio-gasolina.vercel.app/api/cron/collect
